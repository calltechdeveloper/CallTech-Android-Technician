package com.calltechservice.location;/*
package com.courierbuddy.location;


import android.annotation.SuppressLint;
import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.location.Location;

import com.courierbuddy.R;
import com.courierbuddy.utils.CommonUtils;
import com.google.gson.JsonObject;



@SuppressWarnings("ALL")
@SuppressLint("ALL")
public class LocationReceiver extends BroadcastReceiver implements LocationResult {


	@Override
	public void onReceive(Context context, Intent intent) {

		Context mContext = context;
		LocationTracker locationTracker = new LocationTracker(mContext,LocationReceiver.this);
		locationTracker.onUpdateLocation();
		

	}

	@Override
	public void gotLocation(Location location) {

         CommonUtils.printLog(String.valueOf(R.string.location_receiver), String.valueOf(R.string.latitude + location.getLatitude()));


	}


	*/
/*** this method is used for updating location*//*

	private void updateLocation(JsonObject jsonObject) {


	}

	}*/
